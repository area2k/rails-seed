# frozen_string_literal: true

class <%= class_name %>FilterSet < BaseFilterSet
  on <%= class_name %>.table do
    <%- filters.each do |name, filter| -%>
    filter :<%= name %>, <%= filter[:resolver] %>
    <%- end -%>
  end
end
