# frozen_string_literal: true

class <%= class_name %>Filter < Filter
  include FilterTimestamps

  default_table <%= class_name %>.table

  <%- (db_columns - %w[created_at updated_at]).each do |name| -%>
  filter :<%= name %>
  <%- end -%>
end
